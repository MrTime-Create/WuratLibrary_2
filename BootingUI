local BootingUI  = require(script:WaitForChild("Library"))
local Window = Loader:CreateWindow({
	Name = "Test Ui Library",
	LoadingTitleName = "Test UI",
	LoadingTitle = "Test UI Interface Suite",
	IconID = "8990250018", --default ID (Use Roblox ID Only)
	
	ToggleKey = "K",
	
	KeySystem = true,
	WayToGetKey = "Key",
	Key = "Key"
})

local Tab = Window:CreateTab("Test Tab")

Tab:CreateSection("Test Tab", { Color = Color3.fromRGB(255, 255, 255) }) --Use capital letters only

local Button = Tab:CreateButton({
	Name = "Button Example",
	Callback = function()
		print("The button was pressed!")
	end,
})

local Toggle = Tab:CreateToggle({
	Name = "Toggle Example",
	CurrentValue = false,
	Flag = "Toggle1",
	Callback = function(Value)
		print("The toggle was toggled to: " .. tostring(Value))
	end,
})

local Slider = Tab:CreateSlider({
	Name = "Slider Example",
	Range = {0, 100},
	Increment = 1,
	Suffix = "Bananas",
	CurrentValue = 10,
	Flag = "Slider1",
	Callback = function(Value)
		print("The slider was moved to: " .. tostring(Value))
	end,
})

local Input = Tab:CreateInput({
	Name = "Input Example",
	CurrentValue = "",
	PlaceholderText = "Input Placeholder",
	RemoveTextAfterFocusLost = false,
	Flag = "Input1",
	Callback = function(Text)
		print("The input was changed to: " .. tostring(Text))
	end,
})

local Label = Tab:CreateLabel({
	"Label Example", "This is a label."
})

local Dropdown = Tab:CreateDropdown({
	Name = "Dropdown Example",
	Options = {"Option1", "Option2", "Option3", "Option4", "Option5", "Option6", "Option7", "Option8", "Option9", "Option10"},
	CurrentOption = "Option1",
	Flag = "Dropdown1",
	Callback = function(Option)
		print("The dropdown was changed to: " .. tostring(Option))
	end,
})

local Toggle = Tab:CreateToggle({
	Name = "Toggle Example",
	CurrentValue = false,
	Flag = "Toggle1",
	Callback = function(Value)
		print("The toggle was toggled to: " .. tostring(Value))
	end,
})
